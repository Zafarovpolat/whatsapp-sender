services:
  - type: web
    name: whatsapp-sender
    env: docker
    plan: starter # Минимум $7/месяц
    disk:
      name: wa-data
      mountPath: /app/server/data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
